class Project {
    constructor(title, summary, description, element, source_code_url) {
        this.title=title;
        this.summary=summary;
        this.description=description;
        this.element=element;
        this.source_code_url = source_code_url;
    }
}
export default Project;