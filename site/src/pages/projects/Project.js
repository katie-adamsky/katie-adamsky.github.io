class Project {
    constructor(title, summary, description, element) {
        this.title=title;
        this.summary=summary;
        this.description=description;
        this.element=element;
    }
}
export default Project;